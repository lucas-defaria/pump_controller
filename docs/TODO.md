# TODO

- `PumpControl - Resumo de Operação.pdf` foi trazido do branch `main` no merge para `develop-TempControl` mas descreve o **modo MAP-based** (usado pela variante de bomba). Substituir por uma versão equivalente que descreva o modo CAN/temperatura usado por esta variante de fan controller.
